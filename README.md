# html5-spritesheet-builder

A tool using ts-node for building spritesheets for Pixi.js or PlayCanvas

## How to run

* Clone repository
* Run `npm install`
* Place spritesheets in respective folders under `input`
* Run `npm run go`